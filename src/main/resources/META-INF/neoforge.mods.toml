modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"
issueTrackerURL="https://github.com/emmanueltremblay9-stack/Immersive-Farming-Mod-Porting/issues"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
logoFile="logo.png"
credits="Original Immersive Farming by Etyl. Original assets credited to Peter. AstikorCarts credit preserved where compatibility/history is relevant. Includes MIT-licensed code and assets adapted from Immersive Cooking & Farming by akki697222; Farmer's Delight-derived portions retain the original vectorwing copyright notice."
authors="${mod_authors}"
description='''${mod_description}'''

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="${neo_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="immersiveengineering"
    type="required"
    versionRange="${immersiveengineering_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="astikorcarts"
    type="optional"
    versionRange="${optional_compat_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="astikorcartsredux"
    type="optional"
    versionRange="${optional_compat_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="astikorredux"
    type="optional"
    versionRange="${optional_compat_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="jei"
    type="optional"
    versionRange="[19.27.0,)"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="farmersdelight"
    type="optional"
    versionRange="${farmersdelight_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="farm_and_charm"
    type="optional"
    versionRange="[1.1.0,)"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="vinery"
    type="optional"
    versionRange="${optional_compat_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="architectury"
    type="optional"
    versionRange="[13.0.0,)"
    ordering="AFTER"
    side="BOTH"
